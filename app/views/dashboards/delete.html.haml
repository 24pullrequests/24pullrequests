%h1=t("delete_account.title")

%p=t("delete_account.are_you_sure")

%p=t("delete_account.notification_info", email_preferences_link: link_to('email preferences', preferences_path))

%p
  = link_to t("delete_account.confirm"), dashboard_path, :method => :delete, :class => 'btn'
  or
  = link_to t("delete_account.cancel"), dashboard_path
