var projects = $("<%= escape_javascript(render(partial: 'projects/editable_project', collection: @projects, as: :project, locals: { admin: true })) %>")
$("#projects").empty();
$("#projects").append(projects);
