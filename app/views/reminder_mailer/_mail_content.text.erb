Hi <%= @user.nickname %>,

Only <%= 25 - Time.zone.today.day %> <% if (25 - Time.zone.today.day) > 1 %>days<%else%>day<%end%> left until Christmas!

We've got some suggested projects for you to contribute to <%= this_time %>

<% if @user.pull_requests.year(Tfpullrequests::Application.current_year).any? %>
You can view the contributions you've made so far at <%= dashboard_url %>.
<% else %>
You haven't made any contributions yet. Once you make a contribution, you can view your progress at <%= dashboard_url %>.
<% end %>

You can customise which types of project we suggest by setting your favourite languages in the preferences area here: <%= preferences_url %>

Suggested Projects

<% @suggested_projects.each do |project| %>
<%= project.name %> (<%= project.main_language %>)
<%= project.github_url %>
<%= project.description %>

<% end %>
<%= render partial: 'reminder_mailer/sign_off_msg' %>

See you <%= next_time %>!
