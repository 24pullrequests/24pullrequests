%li.user
  = link_to image_tag(gravatar_url(user.gravatar_id), :width => 80, :height => 80, :alt =>user.nickname), user.nickname, rel: 'tooltip', data: { original_title: "#{user.nickname} - #{user.pull_requests.year(current_year).count} pull requests." }
